<%= form_for @item do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label "所属panel" %>
  <%= f.collection_select(:panel_id, Panel.all, :id, :name) %>


  <%= f.submit "Save Changes" %>

<% end %>